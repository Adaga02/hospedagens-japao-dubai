
        // Global data store
        let accommodations = {
            cities: [],
            people: [
                "Lud", "Laura", "Lia", "Mitiko", "Cristina", "Rogerio",
                "Luciana", "Davi", "Arthur", "Suyenne", "Emily", "Yui", "Iuri"
            ]
        };

        // Utility functions
        function formatDate(date) {
            const d = new Date(date);
            return d.toLocaleDateString('pt-BR');
        }
        /* ... resto do JS original ... */
